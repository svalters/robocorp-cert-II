tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir outputx --logtitle "Task log" tasks.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
